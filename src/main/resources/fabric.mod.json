{
	"schemaVersion": 1,
	"id": "${modId}",
	"version": "${version}",
	"name": "${modName}",
	"description": "${modDescription}",
	"authors": [
		"${authors}"
	],
	"contributors": [
		"${contributors}"
	],
	"contact": {
		"homepage": "${homepage}",
		"issues": "${issues}",
		"sources": "${sources}"
	},
	"license": "${license}",
	"icon": "assets/${modId}/icon.png",
	"mixins": [
		"${modId}.mixins.json"
	],
	"depends": {
		"minecraft": ">=${minecraftVersion}",
		"fabricloader": ">=${fabricLoaderVersion}",
		"fabric-api": ">=${fabricApiVersion}"
	},
	"suggests": {
	},
	"entrypoints": {
		"main": [
          "io.github.tommyrobot666.productivecherrytrees.ProductiveCherryTrees"
		],
		"client": [
		]
	}
}
